global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "rxpredict-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["api:8000"]
        labels:
          service: "rxpredict"
          environment: "production"
    scrape_interval: 5s
